#include<bits/stdc++.h>
using namespace std;

int main()
{
    int n,k;
    cin >> n >> k;
    vector<pair<char,int>> c(n);
    for(int i = 0;i < n;i++)
    {
        cin >> c[i].first >> c[i].second;
    }
    
}